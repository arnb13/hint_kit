include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # The example is documentation: it is read more than it is imported.
    public_member_api_docs: ignore
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
